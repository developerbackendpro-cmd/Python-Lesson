#-------------------------------------------------------------------------------------------------------------------------------#
# Python’dagi tuple — bu tartiblangan (ordered) va o‘zgarmas (immutable) elementlar to‘plami.
# Ya’ni, yaratib bo‘linganidan keyin elementlarini o‘zgartirib, qo‘shib yoki o‘chirib bo‘lmaydi.
# ✅ 1. count()
# count(value) → Ko‘rsatilgan qiymat tuple ichida nechta marta borligini qaytaradi
# Python tuple — это упорядоченная (ordered) и неизменяемая (immutable) коллекция элементов.
# То есть после создания элементы нельзя изменять, добавлять или удалять.
# ✅ 1. count()
# count(value) → возвращает, сколько раз указанное значение встречается в tuple.
raqamlar = (1, 2, 2, 3, 2)
print(raqamlar.count(2))
# 💬 2 soni tuple ichida 3 marta mavjud / Число 2 встречается в tuple 3 раза
#-------------------------------------------------------------------------------------------------------------------------------#
# ✅ 2. index()
# index(value) → Qiymat qayerda birinchi marta uchrashini qaytaradi (indeksini)
# ✅ 2. index()
# index(value) → возвращает индекс первого появления указанного значения.
mevalar = ('olma', 'banan', 'olma', 'gilos')
print(mevalar.index('olma'))  # 0
# 💬 olma birinchi marta 0-indexda joylashgan / «olma» впервые находится по индексу 0.
#-------------------------------------------------------------------------------------------------------------------------------#
# 📊 Официальный итоговый список:
# | № |   Metod     | Vazifasi (UZ)                                   | Функция (RU)                                             |
# | - | ----------- | ----------------------------------------------- | -------------------------------------------------------- |
# | 1 |   count()   | Berilgan qiymat nechta marta uchrashini sanaydi | Подсчитывает, сколько раз встречается указанное значение |
# | 2 |   index()   | Qiymatning birinchi uchrash indeksini qaytaradi | Возвращает индекс первого появления значения             |
#-------------------------------------------------------------------------------------------------------------------------------#