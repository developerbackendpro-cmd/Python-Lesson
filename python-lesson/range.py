# 🔢 4️⃣ range (sonlar oralig‘i)
# range — bu tartibli, o‘zgarmas sonlar ketma-ketligi.
# U ko‘pincha sikllarda ishlatiladi, lekin atributlari ham mavjud.
# 🔢 4️⃣ range (диапазон чисел)
# range — это упорядоченная, неизменяемая последовательность чисел.
# Он часто используется в циклах, но также имеет собственные атрибуты.
r = range(2, 10, 2)
#-------------------------------------------------------------------------------------------------------------------------------#
# start – boshlanish soni
# start — начальное число
print(r.start)
# 2
#-------------------------------------------------------------------------------------------------------------------------------#
# stop – to‘xtash qiymati
# stop — конечное значение
print(r.stop)
# 10
#-------------------------------------------------------------------------------------------------------------------------------#
# step – qadam qiymati
# step — величина шага
print(r.step)
# 2
#-------------------------------------------------------------------------------------------------------------------------------#
# len() – nechta element borligini qaytaradi
# len() — возвращает количество элементов.
print(len(r))
# 4
#-------------------------------------------------------------------------------------------------------------------------------#
# | № | Atribut/Metod | Tavsif (UZ)                                     | Описание (RU)                               |
# | - | ------------- | ----------------------------------------------- | ------------------------------------------- |
# | 1 |   start       | Ketma-ketlikning boshlanish soni                | Начальное число диапазона                   |
# | 2 |   stop        | Ketma-ketlik to‘xtaydigan qiymat (kiritilmaydi) | Конечное значение диапазона (не включается) |
# | 3 |   step        | Qadam qiymati (har safar nechaga oshadi)        | Величина шага (на сколько увеличивается)    |
# | 4 |   len()       | Range ichida nechta element borligini qaytaradi | Возвращает количество элементов в диапазоне |
#-------------------------------------------------------------------------------------------------------------------------------#