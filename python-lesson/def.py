# Funksiya – bu biror ishni bajaradigan kod blokidir.
# Vazifalari va ishlatilishi:
# 1. Kodni qayta-qayta ishlatmaslik uchun.
# 2. Katta dasturlarni bo‘laklarga ajratish.
# 3. Murakkab hisob-kitoblarni yoki ish jarayonlarini soddalashtirish.
# 4. Parametrlar yordamida turli qiymatlar bilan ishlash.
# 5. Natija qaytarish yoki ma’lumotni tahlil qilish.
#-------------------------------------------------------------------------------------------------------------------------------#
# Функция — это блок кода, выполняющий определённое действие.
# Назначение и применение:
# 1. Чтобы не повторять один и тот же код многократно.
# 2. Чтобы разделять большие программы на логические части.
# 3. Чтобы упростить сложные расчёты или рабочие процессы.
# 4. Чтобы работать с разными значениями с помощью параметров.
# 5. Чтобы возвращать результат или анализировать данные.
#-------------------------------------------------------------------------------------------------------------------------------#
# 🟢 1️⃣ Oddiy (parametrsiz, qiymat qaytarmaydigan) funksiya / Обычная функция (без параметров и без возвращаемого значения)
def salom_ber():
    print("Assalomu alaykum, talaba !")
salom_ber()
# Natija:
# Assalomu alaykum, talaba!
# ☑️ Funksiya hech qanday ma’lumot olmaydi, faqat xabar chiqaradi / Функция не принимает никаких данных, она просто выводит сообщение.
#-------------------------------------------------------------------------------------------------------------------------------#
# 🟢 2️⃣ Parametrli, qiymat qaytarmaydigan funksiya / Параметризированная функция, не возвращающая значение
def salom_ber(ism):
    print("Salom,", ism)
salom_ber("Dilshod")
# Natija:
# Salom, Dilshod
# ☑️ Parametr sifatida ism olinadi va chiqariladi / Параметр в виде имени принимается и выводится
#-------------------------------------------------------------------------------------------------------------------------------#
# 🟢 3️⃣ Ikkita sonni qo‘shuvchi funksiya / Функция, которая складывает два числа
def yigindi(a, b):
    natija = a + b
    print("Yig'indi:", natija)
yigindi(3, 5)
# Natija:
# Yig‘indi: 8
# ☑️ Funksiya ikki sonni qabul qiladi va ularning yig‘indisini chiqaradi / Функция принимает два числа и выводит их сумму.
#-------------------------------------------------------------------------------------------------------------------------------#
# 🟢 4️⃣ Sukut (default) qiymatli funksiya / Функция со значением по умолчанию
def salom_ber(ism="Mehmon"):
    print("Salom,", ism)
salom_ber()
salom_ber("Aziza")
# Natija:
# Salom, Mehmon
# Salom, Aziza
# ☑️ Agar parametr uzatilmasa — “Mehmon” ishlatiladi. / Если параметр не передан — используется «Гость».
#-------------------------------------------------------------------------------------------------------------------------------#
# 🟢 5️⃣ *args bilan (ko‘p sonlarni qo‘shish) / С использованием *args (сложение нескольких чисел)
def yigindi(*sonlar):
    natija = sum(sonlar)
    print("Jami:", natija)
yigindi(2, 3, 5, 10)
# Natija:
# Jami: 20
# ☑️ sum() barcha sonlarni qo‘shadi /  sum() складывает все числа
#-------------------------------------------------------------------------------------------------------------------------------#
# 🟢 6️⃣ **kwargs bilan (ma’lumotlarni chiqarish) / с использованием **kwargs (вывод данных)
def talaba_info(**malumot):
    print(malumot)
talaba_info(ism="Dilshod", yosh=56, kurs=4)
# Natija:
# {'ism': 'Dilshod', 'yosh': 56, 'kurs': 4}
# ☑️ **malumot argumentlarni lug‘at (dict) ko‘rinishida oladi / принимает информационные аргументы в виде словаря (dict)
#-------------------------------------------------------------------------------------------------------------------------------#
# 🟢 7️⃣  Lambda (sodda bir qatorli funksiya) / Lambda (простая однострочная функция)
salom = lambda ism: print("Salom,", ism)
salom("Olim")
# Izoh: lambda qisqa anonim funksiya,
#-------------------------------------------------------------------------------------------------------------------------------#
# 🟢 8️⃣ Funksiya ichida funksiya (soddalashtirilgan) / «Функция внутри функции (упрощённо)»
def tashqi():
    def ichki():
        print("Ichki funksiya ishladi")
    ichki()
tashqi()
# Natija:
# Ichki funksiya ishladi
# ☑️ Ichki funksiya faqat tashqi funksiyada ishlaydi. / «Внутренняя функция работает только внутри внешней функции.»
#-------------------------------------------------------------------------------------------------------------------------------#
# 🟢 9️⃣ Funksiyani boshqa funksiyaga uzatish / «Передача функции в другую функцию»
def salom_ayt():
    print("Assalomu alaykum!")
def bajar(f):
    f()
bajar(salom_ayt)
# Natija:
# Assalomu alaykum!
# ☑️ Bu yerda funksiya boshqa funksiyaga uzatilgan va ichida chaqirilgan. / «Здесь функция передана другой функции и вызвана внутри неё.»
#-------------------------------------------------------------------------------------------------------------------------------#
# 🟢 🔟 Rekursiv funksiya (soddalashtirilgan)
# Bu funksiya o‘zini ichida chaqiradi, bu juda muhim mavzu (ayniqsa faktorial, fibonachchi kabi misollarda).
# Va bu Python’dagi funksiyalarning o‘rta-darajadagi turi hisoblanadi.
#-------------------------------------------------------------------------------------------------------------------------------#
# 🟢 🔟 Рекурсивная функция (упрощённо)
# Эта функция вызывает саму себя, что является очень важной темой (особенно в примерах, таких как факториал и числа Фибоначчи).
# И это считается функцией среднего уровня в Python.
#-------------------------------------------------------------------------------------------------------------------------------#
def hisobla(son):
    if son > 0:
        print(son)
        hisobla(son - 1)  # o‘zini qayta chaqiradi / вызывает саму себя
hisobla(5)
# Natija:
# 5
# 4
# 3
# 2
# 1
# ☑️ Funksiya o‘zini ichida chaqirib, sonni kamaytirib chiqaradi.
# Har chaqirilganda son 1 ga kamayadi, 0 bo‘lganda to‘xtaydi.
# ☑️ Функция вызывает саму себя, уменьшая число при каждом вызове.
# При каждом вызове число уменьшается на 1, а при достижении 0 выполнение прекращается.
#-------------------------------------------------------------------------------------------------------------------------------#